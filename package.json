{
  "name": "simple-node-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-server": "cd server && npm install && npm run tsc",
    "build-client": "cd client && npm install && npm run build",
    "start": "npm run build-client && npm run build-server && docker-compose up --build -V"
  },
  "author": "",
  "license": "ISC"
}
